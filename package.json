{
  "name": "pega-bola",
  "version": "1.0.0",
  "description": "Pega Bola 3000",
  "main": "dist/server.js",
  "author": "Riquelme",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "bun run build && node dist/server.js",
    "start:watch": "bun run build && node --watch dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:prod": "bash scripts/deploy-production.sh"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.4"
  }
}
